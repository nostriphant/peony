# Peony Media Server
Blossom Media Server implementation for shared web servers

# Installation instructions
Assuming a apache webserver

```
1. git clone https://github.com/nostriphant/peony
2. configure .env (see example.env)
3. point document root to ./peony/public
4. configure blossom media server in your favorite Nostr client

```